<UserControl x:Class="Atomus.Windows.Controls.Toolbar.DefaultToolbar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="119" d:DesignWidth="990"
             Loaded="UserControl_Loaded">
    <Grid>
        <!-- Ribbon Region -->
        <Ribbon x:Name="ribbon" Grid.ColumnSpan="2" ShowQuickAccessToolBarOnTop="False"  Background="{Binding BackColor}" Foreground="{Binding ForeColor}" BorderBrush="{Binding BorderColor}">
            <Ribbon.QuickAccessToolBar>
                <RibbonQuickAccessToolBar x:Name="ribbonQuickAccessToolBar">
                </RibbonQuickAccessToolBar>
            </Ribbon.QuickAccessToolBar>

            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu Visibility="Collapsed">
                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>
        </Ribbon>
    </Grid>
</UserControl>